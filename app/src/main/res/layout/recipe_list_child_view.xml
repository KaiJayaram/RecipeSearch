<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_green_dark"
    android:orientation="vertical"
    android:weightSum="1">

    <ImageView
        android:id="@+id/imageSwitch"
        android:layout_width="71dp"
        android:layout_height="62dp"
        android:layout_alignBottom="@+id/recipeSelect" />

    <TextView
        android:id="@+id/recipeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/imageSwitch"
        android:layout_toLeftOf="@+id/recipeSelect"
        android:layout_toRightOf="@+id/imageSwitch"
        android:layout_toStartOf="@+id/recipeSelect"
        android:background="@android:color/holo_green_dark"
        android:text="Test Recipe"
        android:textAppearance="@android:style/TextAppearance.Material.WindowTitle" />

    <Button
        android:id="@+id/recipeSelect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="@android:color/holo_blue_dark"
        android:text="Select" />
</RelativeLayout>